diff --git a/dist/component/convex.config.js b/dist/component/convex.config.js
index 3d15ed6b9a71d4f2e29f846cd61b4d85531ff13a..ebe67b450fe3ea8cc37f70f63f5f56b68461169f 100644
--- a/dist/component/convex.config.js
+++ b/dist/component/convex.config.js
@@ -1,6 +1,13 @@
 import { defineComponent } from "convex/server";
+import { v } from "convex/values";
 import workpool from "@convex-dev/workpool/convex.config";
-const component = defineComponent("agentmail");
+const component = defineComponent("agentmail", {
+    env: {
+        AGENTMAIL_API_KEY: v.optional(v.string()),
+        AGENTMAIL_BASE_URL: v.optional(v.string()),
+        AGENTMAIL_WEBHOOK_SECRET: v.optional(v.string()),
+    },
+});
 component.use(workpool, { name: "sendPool" });
 component.use(workpool, { name: "callbackPool" });
 export default component;
diff --git a/dist/component/eventLogic.js b/dist/component/eventLogic.js
index af84fa750defb684c213ad4607dc3fc517500548..825b917eda5d8e78207528cab005adaee170a419 100644
--- a/dist/component/eventLogic.js
+++ b/dist/component/eventLogic.js
@@ -57,6 +57,7 @@ export function mapEventToOutboundStatus(eventType) {
         case "message.rejected":
             return "rejected";
         case "message.received":
+        case "message.received.unauthenticated":
         case "domain.verified":
             return undefined;
     }
diff --git a/dist/component/lib.js b/dist/component/lib.js
index c245802ccb5cd51a38444b50c0310f65cc5cf285..4e8487466547655f011fe5debee40da42ab35487 100644
--- a/dist/component/lib.js
+++ b/dist/component/lib.js
@@ -1,5 +1,5 @@
 import { v } from "convex/values";
-import { internalAction, internalMutation, internalQuery, mutation, query, } from "./_generated/server.js";
+import { action, internalAction, internalMutation, internalQuery, mutation, query, } from "./_generated/server.js";
 import { components, internal } from "./_generated/api.js";
 import { Workpool } from "@convex-dev/workpool";
 import { vRuntimeConfig, vSendKind, vSendPayload, } from "./shared.js";
@@ -17,7 +17,7 @@ const callbackPool = new Workpool(components.callbackPool, {
 // ---------------------------------------------------------------------------
 // Inboxes
 // ---------------------------------------------------------------------------
-export const createInbox = internalAction({
+export const createInbox = action({
     args: {
         request: v.object({
             username: v.optional(v.string()),
@@ -35,7 +35,7 @@ export const createInbox = internalAction({
         return inbox;
     },
 });
-export const listInboxes = internalAction({
+export const listInboxes = action({
     args: {
         limit: v.optional(v.number()),
         page_token: v.optional(v.string()),
@@ -52,7 +52,7 @@ export const listInboxes = internalAction({
         });
     },
 });
-export const getInboxRemote = internalAction({
+export const getInboxRemote = action({
     args: { inboxId: v.string() },
     handler: async (ctx, args) => {
         const inbox = (await agentmailFetch(`/inboxes/${args.inboxId}`, {
@@ -62,7 +62,7 @@ export const getInboxRemote = internalAction({
         return inbox;
     },
 });
-export const deleteInbox = internalAction({
+export const deleteInbox = action({
     args: { inboxId: v.string() },
     handler: async (ctx, args) => {
         await agentmailFetch(`/inboxes/${args.inboxId}`, {
@@ -310,7 +310,7 @@ export const getOutboundStatus = query({
 // ---------------------------------------------------------------------------
 // Threads / messages: thin wrappers over remote API
 // ---------------------------------------------------------------------------
-export const listThreads = internalAction({
+export const listThreads = action({
     args: {
         inboxId: v.string(),
         limit: v.optional(v.number()),
@@ -332,13 +332,13 @@ export const listThreads = internalAction({
         });
     },
 });
-export const getThread = internalAction({
+export const getThread = action({
     args: { inboxId: v.string(), threadId: v.string() },
     handler: async (_ctx, args) => {
         return await agentmailFetch(`/inboxes/${args.inboxId}/threads/${args.threadId}`, { method: "GET" });
     },
 });
-export const getMessage = internalAction({
+export const getMessage = action({
     args: { inboxId: v.string(), messageId: v.string() },
     handler: async (_ctx, args) => {
         return await agentmailFetch(`/inboxes/${args.inboxId}/messages/${args.messageId}`, { method: "GET" });
@@ -386,8 +386,14 @@ export const handleEvent = mutation({
             receivedAt: Date.now(),
             raw: event,
         });
-        if (event.event_type === "message.received" && event.message) {
-            const m = event.message;
+        if ((event.event_type === "message.received" ||
+            event.event_type === "message.received.unauthenticated") &&
+            event.message) {
+            // Preserve the provider's trust classification even if labels were omitted.
+        if (event.event_type === "message.received.unauthenticated") {
+            event.message = { ...event.message, labels: [...(Array.isArray(event.message.labels) ? event.message.labels : []), "unauthenticated"] };
+        }
+        const m = event.message;
             await ctx.db.insert("inboundMessages", {
                 inboxId: m.inbox_id,
                 threadId: m.thread_id,
@@ -417,7 +423,8 @@ export const handleEvent = mutation({
             await callbackPool.enqueueMutation(ctx, handle, { event });
         }
         if (args.config.onMessageReceived &&
-            event.event_type === "message.received") {
+            (event.event_type === "message.received" ||
+                event.event_type === "message.received.unauthenticated")) {
             const handle = args.config.onMessageReceived
                 .fnHandle;
             await callbackPool.enqueueMutation(ctx, handle, {
diff --git a/dist/component/shared.d.ts b/dist/component/shared.d.ts
index 4a2e87760fe7624eee74d5c78f67e7a4fd12b55f..d5aa7e69f3a89f0a2c3a77a232f6c0c09d6076c8 100644
--- a/dist/component/shared.d.ts
+++ b/dist/component/shared.d.ts
@@ -56,7 +56,7 @@ export declare const vSendPayload: import("convex/values").VObject<{
     reply_all: import("convex/values").VBoolean<boolean | undefined, "optional">;
 }, "required", "reply_all" | "labels" | "reply_to" | "to" | "cc" | "bcc" | "subject" | "text" | "html" | "attachments" | "headers" | `headers.${string}`>;
 export type SendPayload = Infer<typeof vSendPayload>;
-export declare const vEventType: import("convex/values").VUnion<"message.received" | "message.sent" | "message.delivered" | "message.bounced" | "message.complained" | "message.rejected" | "domain.verified", [import("convex/values").VLiteral<"message.received", "required">, import("convex/values").VLiteral<"message.sent", "required">, import("convex/values").VLiteral<"message.delivered", "required">, import("convex/values").VLiteral<"message.bounced", "required">, import("convex/values").VLiteral<"message.complained", "required">, import("convex/values").VLiteral<"message.rejected", "required">, import("convex/values").VLiteral<"domain.verified", "required">], "required", never>;
+export declare const vEventType: import("convex/values").VUnion<"message.received" | "message.received.unauthenticated" | "message.sent" | "message.delivered" | "message.bounced" | "message.complained" | "message.rejected" | "domain.verified", [import("convex/values").VLiteral<"message.received", "required">, import("convex/values").VLiteral<"message.received.unauthenticated", "required">, import("convex/values").VLiteral<"message.sent", "required">, import("convex/values").VLiteral<"message.delivered", "required">, import("convex/values").VLiteral<"message.bounced", "required">, import("convex/values").VLiteral<"message.complained", "required">, import("convex/values").VLiteral<"message.rejected", "required">, import("convex/values").VLiteral<"domain.verified", "required">], "required", never>;
 export type EventType = Infer<typeof vEventType>;
 export declare const vEvent: import("convex/values").VObject<{
     send?: any;
@@ -68,11 +68,11 @@ export declare const vEvent: import("convex/values").VObject<{
     reject?: any;
     domain?: any;
     type: "event";
-    event_type: "message.received" | "message.sent" | "message.delivered" | "message.bounced" | "message.complained" | "message.rejected" | "domain.verified";
+    event_type: "message.received" | "message.received.unauthenticated" | "message.sent" | "message.delivered" | "message.bounced" | "message.complained" | "message.rejected" | "domain.verified";
     event_id: string;
 }, {
     type: import("convex/values").VLiteral<"event", "required">;
-    event_type: import("convex/values").VUnion<"message.received" | "message.sent" | "message.delivered" | "message.bounced" | "message.complained" | "message.rejected" | "domain.verified", [import("convex/values").VLiteral<"message.received", "required">, import("convex/values").VLiteral<"message.sent", "required">, import("convex/values").VLiteral<"message.delivered", "required">, import("convex/values").VLiteral<"message.bounced", "required">, import("convex/values").VLiteral<"message.complained", "required">, import("convex/values").VLiteral<"message.rejected", "required">, import("convex/values").VLiteral<"domain.verified", "required">], "required", never>;
+    event_type: import("convex/values").VUnion<"message.received" | "message.received.unauthenticated" | "message.sent" | "message.delivered" | "message.bounced" | "message.complained" | "message.rejected" | "domain.verified", [import("convex/values").VLiteral<"message.received", "required">, import("convex/values").VLiteral<"message.received.unauthenticated", "required">, import("convex/values").VLiteral<"message.sent", "required">, import("convex/values").VLiteral<"message.delivered", "required">, import("convex/values").VLiteral<"message.bounced", "required">, import("convex/values").VLiteral<"message.complained", "required">, import("convex/values").VLiteral<"message.rejected", "required">, import("convex/values").VLiteral<"domain.verified", "required">], "required", never>;
     event_id: import("convex/values").VString<string, "required">;
     message: import("convex/values").VAny<any, "optional", string>;
     thread: import("convex/values").VAny<any, "optional", string>;
diff --git a/dist/component/shared.js b/dist/component/shared.js
index 1586218fe83098ed1121dcc2642881590ccea9e4..f4a25da6b538ee791d409f0861148adef0eeb779 100644
--- a/dist/component/shared.js
+++ b/dist/component/shared.js
@@ -20,7 +20,7 @@ export const vSendPayload = v.object({
     headers: v.optional(v.record(v.string(), v.string())),
     reply_all: v.optional(v.boolean()),
 });
-export const vEventType = v.union(v.literal("message.received"), v.literal("message.sent"), v.literal("message.delivered"), v.literal("message.bounced"), v.literal("message.complained"), v.literal("message.rejected"), v.literal("domain.verified"));
+export const vEventType = v.union(v.literal("message.received"), v.literal("message.received.unauthenticated"), v.literal("message.sent"), v.literal("message.delivered"), v.literal("message.bounced"), v.literal("message.complained"), v.literal("message.rejected"), v.literal("domain.verified"));
 export const vEvent = v.object({
     type: v.literal("event"),
     event_type: vEventType,
diff --git a/src/component/convex.config.ts b/src/component/convex.config.ts
index b1cc7f157e4bf2c31dde9a439b9ae6d66384e2fe..96b47701d233d899caa832ef5fe069acde6df1eb 100644
--- a/src/component/convex.config.ts
+++ b/src/component/convex.config.ts
@@ -1,7 +1,14 @@
 import { defineComponent } from "convex/server";
+import { v } from "convex/values";
 import workpool from "@convex-dev/workpool/convex.config";
 
-const component = defineComponent("agentmail");
+const component = defineComponent("agentmail", {
+  env: {
+    AGENTMAIL_API_KEY: v.optional(v.string()),
+    AGENTMAIL_BASE_URL: v.optional(v.string()),
+    AGENTMAIL_WEBHOOK_SECRET: v.optional(v.string()),
+  },
+});
 component.use(workpool, { name: "sendPool" });
 component.use(workpool, { name: "callbackPool" });
 
diff --git a/src/component/eventLogic.ts b/src/component/eventLogic.ts
index 00c5328e76071b5bc335065da77b4454be207ccc..43207e81b81f23ccb752309e10e4b6a02b6e80a3 100644
--- a/src/component/eventLogic.ts
+++ b/src/component/eventLogic.ts
@@ -71,6 +71,7 @@ export function mapEventToOutboundStatus(
     case "message.rejected":
       return "rejected";
     case "message.received":
+    case "message.received.unauthenticated":
     case "domain.verified":
       return undefined;
   }
diff --git a/src/component/lib.ts b/src/component/lib.ts
index 121dd580ef3d608ca8c76b7d685fa5e1cb6d402b..c4bbefe6a6a4f9773d5fbcbf85648ddab21fdbb8 100644
--- a/src/component/lib.ts
+++ b/src/component/lib.ts
@@ -1,5 +1,6 @@
 import { v } from "convex/values";
 import {
+  action,
   internalAction,
   internalMutation,
   internalQuery,
@@ -40,7 +41,7 @@ const callbackPool = new Workpool(components.callbackPool, {
 // Inboxes
 // ---------------------------------------------------------------------------
 
-export const createInbox = internalAction({
+export const createInbox = action({
   args: {
     request: v.object({
       username: v.optional(v.string()),
@@ -59,7 +60,7 @@ export const createInbox = internalAction({
   },
 });
 
-export const listInboxes = internalAction({
+export const listInboxes = action({
   args: {
     limit: v.optional(v.number()),
     page_token: v.optional(v.string()),
@@ -77,7 +78,7 @@ export const listInboxes = internalAction({
   },
 });
 
-export const getInboxRemote = internalAction({
+export const getInboxRemote = action({
   args: { inboxId: v.string() },
   handler: async (ctx, args) => {
     const inbox = (await agentmailFetch(`/inboxes/${args.inboxId}`, {
@@ -88,7 +89,7 @@ export const getInboxRemote = internalAction({
   },
 });
 
-export const deleteInbox = internalAction({
+export const deleteInbox = action({
   args: { inboxId: v.string() },
   handler: async (ctx, args) => {
     await agentmailFetch(`/inboxes/${args.inboxId}`, {
@@ -362,7 +363,7 @@ export const getOutboundStatus = query({
 // Threads / messages: thin wrappers over remote API
 // ---------------------------------------------------------------------------
 
-export const listThreads = internalAction({
+export const listThreads = action({
   args: {
     inboxId: v.string(),
     limit: v.optional(v.number()),
@@ -385,7 +386,7 @@ export const listThreads = internalAction({
   },
 });
 
-export const getThread = internalAction({
+export const getThread = action({
   args: { inboxId: v.string(), threadId: v.string() },
   handler: async (_ctx, args) => {
     return await agentmailFetch(
@@ -395,7 +396,7 @@ export const getThread = internalAction({
   },
 });
 
-export const getMessage = internalAction({
+export const getMessage = action({
   args: { inboxId: v.string(), messageId: v.string() },
   handler: async (_ctx, args) => {
     return await agentmailFetch(
@@ -452,7 +453,15 @@ export const handleEvent = mutation({
       raw: event,
     });
 
-    if (event.event_type === "message.received" && event.message) {
+    if (
+      (event.event_type === "message.received" ||
+        event.event_type === "message.received.unauthenticated") &&
+      event.message
+    ) {
+      if (event.event_type === "message.received.unauthenticated") {
+        const message = event.message as InboundMessagePayload;
+        event.message = { ...message, labels: [...(Array.isArray(message.labels) ? message.labels : []), "unauthenticated"] };
+      }
       const m = event.message as InboundMessagePayload;
       await ctx.db.insert("inboundMessages", {
         inboxId: m.inbox_id,
@@ -489,7 +498,8 @@ export const handleEvent = mutation({
     }
     if (
       args.config.onMessageReceived &&
-      event.event_type === "message.received"
+      (event.event_type === "message.received" ||
+        event.event_type === "message.received.unauthenticated")
     ) {
       const handle = args.config.onMessageReceived
         .fnHandle as FunctionHandle<
diff --git a/src/component/shared.ts b/src/component/shared.ts
index 4c958f53d51d8a0fd9733fc6288f276ee73a0d01..a15deb8c16739978be4bdb0e989e96f3da6012e4 100644
--- a/src/component/shared.ts
+++ b/src/component/shared.ts
@@ -50,6 +50,7 @@ export type SendPayload = Infer<typeof vSendPayload>;
 
 export const vEventType = v.union(
   v.literal("message.received"),
+  v.literal("message.received.unauthenticated"),
   v.literal("message.sent"),
   v.literal("message.delivered"),
   v.literal("message.bounced"),
